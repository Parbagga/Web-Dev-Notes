const num1 = 100;
const num2 = 50;
let val;

// simple math with numbers
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

//math oblect PI
val = Math.PI; //property is an attribute method is function
val = Math.E; //Euler's N umber
val = Math.round(2.8);
val = Math.ceil(2.8);
val = Math.floor(2.8);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.round(2.8);
val = Math.pow(8, 2);
val = Math.min(2, 33, 4, 1, 55, 6, 3, -2); 
val = Math.max(2, 33, 4, 1, 55, 6, 3, -2);
val = Math.random();//give any random number till 0-1
val = Math.random() *20; // max to 20 bet 0-19
val = Math.random() * 20 + 1; // till 20
val = Math.floor(Math.random() * 6 + 1); // till 20 rounded

console.log(val);
